module.exports = function () {
  return {
    all: {
      options: {
        sassDir: 'src',
        cssDir: 'src',
        noLineComments: true,
        outputStyle: 'compressed'
      }
    }
  };
};
